{
  "name": "tipay",
  "private": true,
  "version": "0.1.0",
  "description": "Tipay — accountability sessions on Stellar with XLM 💥",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint",
    "contract:build": "pnpm --filter contracts-soroban build",
    "contract:test": "pnpm --filter contracts-soroban test",
    "contract:deploy:testnet": "pnpm --filter contracts-soroban deploy:testnet"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
